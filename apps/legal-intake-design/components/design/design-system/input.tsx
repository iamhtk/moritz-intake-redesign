/**
 * Design-system shim for Input. The playground standardised on the foundation
 * design system, so this re-exports the foundation Input. Kept as a stable
 * `@/components/design/design-system/input` import path for existing call sites.
 */
export { Input } from '@/components/design/foundations/components/input';
